---
title: دوبلير الاختبار (Test Doubles) في إختبار المكونات
pubDatetime: 2026-01-12T10:00:00Z
description: تعرف على أنواع دوبلير الاختبار المختلفة وكيفية استخدامها في إختبار المكونات (unit testing) لعزل اختباراتك عن التبعيات الخارجية.
---

## 📋متطلبات قبل القراءة

- خبرة سابقة بأي لغة برمجة
- معرفة بسيطة بإختبار المكونات (unit testing)

## 🎯 مقدمة

أحد أشهر أنواع الاختبارات في مجال البرمجيات هو **إختبار المكونات (unit testing)** حيث المكون عادة هو دالة برمجية سواء كانت منفردة او جزء من كائن (method) كما في البرمجة الشيئية (OOP)

عند كتابة إختبار لأي مكون دائما ستجد ان هناك بعض التبعيات التي يعتمد عليها مكونك ، من أمثلتها :

- **نداء API** والذي عادة ما يكون مدفوع و انت عادة سوف تحتاج إلي إجراء إختباراتك مرات عديدة
- **إستعلام من قاعدة البيانات**
- **تسجيل الأحداث (logging)**
- **إرسال بريد إلكترونى**

كلها أشياء تريد عادة تجنبها أثناء إجراء إختباراتك إما لتوفير تكلفة مثل ال api او تجنب حدوث آثار جانبية غير مرغوب فيها مثل تسجيل الأحداث (logs) أو إرسال بريد إلكترونى .

لكن السبب الأهم و الأكثر جوهرية هو من أجل عزل إختبارك و وضعه في بيئة تكون تحت تحكمك بالكامل.

مثال في حالة ان مكونك البرمجي يقوم بإجراء نداء لAPI انسي التكلفة المادية الأسوء هو ان يفشل اختبارك لعدم وجود اتصال انترنت او ان ال API يرجع لك بيانات مختلفة في كل مرة مثل نداء ل API يرجع لك أخبار بلد معين لآخر ٢٤ ساعة او api حالة الطقس و هذا ما لاتريده انت تريد ان تضمن ان اختبارك في كل مرة بتلقى نفس المدخلات و ينتج نفس المخرجات و انه إذا فشل الاختبار سيكون السبب الوحيد هو عيب في منطق المكون أو تنفيذه الذي وليس بسبب تأثير خارجي من البيئة

من أجل ذلك نحتاج الي **دوبلير الاختبار (test double)** المصطلح يأتي من دوبلير في عالم السينما و هو الشخص الذي يقوم بالمشاهد الخطرة نيابة عن الممثل و يطلق عليه بالإنجليزية (stunt double) و منه جاء المصطلح الانجليزي (test double) و علي نفس النهج نحتاج دوبلير لاستبدال أجزاء الأختبار السابق ذكرها لنصنع بيئة محكمة بحيث لا يوجد تدخل من البيئة الخارجية

## أنواع الدوبلير

الدوبلير في إختبار المكونات له ٥ أنواع

### البليد (Dummy)

أفضل وصف له هو دوبلير **سد خانة**

تحتاج هذا النوع عادة من أجل أن توفي بمتطلبات المكون البرمجى لكي يعمل و لكن هذا الدوبلير وظيفته لن تؤثر علي نتيجة الإختبار بأي شكل

مثال:

```js
// util.js
function  calculateSum(num1, num2, logger) {
const sum = num1 + num2;

logger.log(`sum is ${sum}`);
return sum;
}

// utils.test.js
import { describe, it, expect } from 'vitest';

describe('calculateSum', () => {
    it('should calculate the sum of numbers correctly', () => {
        ​const dummyLogger = {
            log: () => {},
        };

        const sum = calculateSum(3, 5, dummyLogger);

        expect(sum).toBe(8);
    });
});
```

لديك مكون يقوم بعملية جمع حسابية و بعد الانتهاء يقوم بتسجيل (log) ناتج العملية

في هذا الاختبار، كل تركيزنا كان منصبًا على منطق الكود نفسه، وهل يقوم بالعملية الحسابية بشكل صحيح أم لا. ولم نهتم بالتسجيل، حتى ولو فشل. كل ما في الأمر أن المكون ليعمل بشكل صحيح، احتاج إلى هذا المعطى `logger` وعليه هذه الدالة `log`، فقمنا بتوفيرها له فقط لسد الخانة لا أكثر.

### مُعلب (Stub)

اخترت هذه الترجمة لكلمة (Stub) لأن هذا الدوبلير يرد عليك برد مُعلب لا يتغير، أو قد يعتمد بشكل بسيط على المعطيات التي تقوم بتوفيرها له. ولكن السمة الأساسية أنه لا يوجد أي منطق بداخله، فقط رد جاهز مباشر.

مثال:

```js
// utils.js
export function getWelcomeMessage(userId, usersDB) {
  const user = usersDB.findById(userId);

  if (user) return `Welcome ${user.name}`;
return 'Welcome undefined';
}

// utils.test.js
import { expect, describe, it, vi } from 'vitest';
import { getWelcomeMessage } from '../src/basic.js';

describe('getWelcomeMessage', () => {
  it('should return welcome message with user name if in db', () => {
    const usersDB = {
      findById: vi.fn((id) => ({
        id: id,
        name: 'Yousef',
      })),
    };

    const message = getWelcomeMessage('user123', usersDB);

    expect(message).toBe('Welcome Yousef');
  });

  it('should return welcome undefined if user not in db', () => {
    ​const usersDB = {
      findById: (id) => undefined,
    };

    const message = getWelcomeMessage('user456', usersDB);

    expect(message).toBe('Welcome undefined');
  });
});
```

كما ترى، لدينا دالة تأخذ معطيين: الأول الرقم التعريفي للمستخدم، والثاني اتصال بقاعدة بيانات المستخدمين. وتحاول أن تجد المستخدم وطباعة رسالة ترحيبية باسم المستخدم إن وجد، أو قيمة مجهولة `undefined` بدل الاسم إذا لم يوجد المستخدم.

فمباشرةً، تستطيع ملاحظة أن جزئية البحث في قاعدة البيانات تمثل مشكلة كبيرة من ناحية عزل الاختبار، حيث يمكن أن يحدث مشكلة في الاتصال بقاعدة البيانات، أو أن يقوم زميل لك بمسح المستخدم الذي تستخدمه في الاختبار. وأنت لا تريد أن يفشل اختبارك بسبب عامل خارجي. فأفضل حل لنا هنا هو استخدام دوبلير مُعلب برد جاهز، كما هو موضح بالمثال.

ولاحظ مرة أخرى كيف أن الرد مُعلب جاهز، ولا يوجد أي نوع من المعالجة للمعطيات.

### جاسوس (Spy)

هذا الدوبلير، على عكس النوعين السابقين، لا يقوم بتغيير منطق المكون المستبدل أو يعدل في الكود بأي شكل. ولكنه يعمل كوسيط بحيث عند نداء الدوبلير، فإنه يمرر النداء للنسخة الأصلية، ولكن يقوم بشيء مفيد جدًا، وهو **التجسس**. كيف؟!

يقوم بتسجيل عدد مرات نداء الدوبلير، والمعطيات في كل مرة، والبيانات المرتجعة لكل نداء. هذا يتيح لك أن تتأكد من تفاعل المكون الذي تختبره مع المكونات الأخرى، وهل استخدمها بالشكل المتوقع منه أم لا.

مثال

```js
// util.js
function  calculateSum(num1, num2, logger) {
const sum = num1 + num2;

logger.log(`sum is ${sum}`);
return sum;
}
// utils.test.js
import { describe, it, expect } from 'vitest';

describe('calculateSum', () => {
    it('should log sum result', () => {
        ​const consoleSpy = vi.spyOn(console, log)

        const sum = calculateSum(3, 5, console);

        expect(sum).toBe(8);

        expect(consoleSpy).toHaveBeenCalledTimes(1)
        expect(consoleSpy).toHaveBeenCalledWith('sum is 8')
    });
});
```

هنا نحن نعيد استخدام المثال الأول للدوبلير البليد، حيث قد اختبرنا أنه يقوم بالعمليات الحسابية بشكل صحيح. والآن نريد أن نتأكد بأنه يقوم بعمل تسجيل بشكل صحيح في آخر العملية.

كمعطى، فقد مررنا كائن `console`، وهو الكائن المسؤول عن الطباعة للترمينال، والجافا سكريبت توفره لنا بشكل افتراضي كمتغير عام دون الحاجة لاستيراده، وعليه دالة `log`.

استخدام دوبلير بليد هنا لن يفيدنا وحده، لأننا لن نعرف في نهاية المطاف هل تم استخدامه أم لا. وهنا تأتي أهمية الدوبلير الجاسوس. فمن خلالة استطعنا وضع توقع بهل تم استخدامه ام لا و بالمعطى الذى نريده أم لا

### المحاكى (Mock)

فمثلا بعد إن شاء محاكي لكائن أو دالة برمجية فأنك من الممكت ان تحدد ان السلوك سيكون ان الدالة أ يتم ندائها أولاا ثم يتم نداء الدالة ب بمعطيات محددة و يجب ان ترجع القيمة الفلانية فإذا لم يتصرف الدوبلير بنفس السلوك فإن الاختبار يفشل.

أشهر دوبلير بينهم، وهذا الذي ستستخدمه كثيرًا. هذا الدوبلير يجمع بين **المُعلب** و**الجاسوس**. حيث تقوم بمحاكاة دالة أو كائن برمجي بالكامل، وتستطيع تغيير الكود أو التجسس أو القيام بالاثنين معًا.

ولكنه ليس فقط خليط من النوعين السابقين، بل يضيف عليهما ما يسمى **التحقق السلوكي** (Behavior Verification). حيث مع كل الدوبليرات السابقين، فهم لا يقرروا نجاح أو فشل الاختبار، ولكن هنا، نعم.

عندما تنشئ دوبلير محاكي، فأنت لديك القدرة لتحدد كيف يتصرف أثناء الاختبار. وإذا لم يتصرف بنفس السلوك الذي حددته، فإن الاختبار يفشل، والعكس صحيح.

مثال:

```js
// utils.js
export class UserManager {
  constructor(userRepository, emailService) {
    this.userRepository = userRepository;
    this.emailService = emailService;
  }

  registerUser(user) {
    if (this.userRepository.findByEmail(user.email)) {
      throw new Error("البريد الإلكتروني مستخدم بالفعل");
    }

    this.userRepository.save(user);

    this.emailService.sendWelcomeEmail(user.email);
  }
}

// utils.test.js
import { expect, describe, it, vi } from "vitest";
import { UserManager } from "./utils.js";

describe("UserManager", () => {
  it("should save the user and send a welcome email", () => {
    const mockUserRepository = {
      findByEmail: vi.fn(() => null),
      save: vi.fn(),
    };

    const mockEmailService = {
      sendWelcomeEmail: vi.fn(),
    };

    const userManager = new UserManager(mockUserRepository, mockEmailService);

    const newUser = { email: "test@example.com", name: "Test User" };

    userManager.registerUser(newUser);

    expect(mockUserRepository.findByEmail).toHaveBeenCalledExactlyOnceWith(
      newUser.email
    );
    expect(mockUserRepository.save).toHaveBeenCalledWith(newUser);
    expect(mockUserRepository.save).toHaveBeenCalledAfter(
      mockUserRepository.findByEmail
    );
    expect(mockEmailService.sendWelcomeEmail).toHaveBeenCalledAfter(
      mockUserRepository.save
    );
    expect(mockEmailService.sendWelcomeEmail).toHaveBeenCalledWith(
      "test@example.com"
    );
  });
});
```

في المثال لدينا كائن `userManager` المسئول عن عملية تسجيل المستخدمين و عليه بعض الدوال ، و نريد ان نتأكد انه يتصرف بنفس الطريقة الذى نظنه يتصرف بها

لإنشاء محاكى لدالة في إطار عمل [فيتست](https://vitest.dev/) الخاص بالجافا سكريبت نقوم باستخدام `vi.fn`. ثم يمكنك ان تري من مجموعة التوقعات التى كتبناها كيف اننا نتحقق من سلوك كائن `userManager` عند التسجيل

قد تتسأل بأن بعض الدوال المستخدمة هنا هى المستخدمة مع الجاسوس و هذا صحيح و لكن هذا بسبب إطار العمل المستخدم [فيتست](https://vitest.dev/) سنتحدث عن هذا لاحقا

### المزيف (Fake)

دوبلير مشابه للدوبلير المُعلب، الفرق أنه لديه منطق وكود ينفذ هذا المنطق، ولكنه منطق أبسط وتنفيذ أبسط من المكون الحقيقي الذي يعتمد عليه مكوننا تحت الاختبار.

مثال:

```js
// utils.js
function addUser(user, usersDB) {
  if (usersDB.findByEmail(user.email)) {
    throw new Error("البريد الإلكتروني مستخدم بالفعل");
  }

  usersDB.save(user);
}
// utils.test.js
import { expect, describe, it } from "vitest";

describe("addUser", () => {
  it("should add a new user to the db", () => {
    const newUser = { email: "test@example.com", name: "Test User" };

    const fakeUserDB = {
      users: [],
      findByEmail(email) {
        return this.users.find(user => user.email === email);
      },
      save(user) {
        this.users.push(user);
      },
    };

    addUser(newUser, fakeUserDB);

    const savedUser = fakeUserDB.findByEmail("test@example.com");
    expect(savedUser).toEqual(newUser);
  });
});
```

بدل من اتصال لقاعدة بيانات و حفظ هذه البيانات علي الحاسب ممكن ان ننشأ دوبلير يقوم بنفس المهمة ولكن في الذاكرة

## 🫒 الزيتونة

التعريفات السابقة ليست قياسية، ولا توجد جهة معتمدة وضعتها، ولكنها المتعارف عليها. لذلك، قد تجد بعض الآراء المختلفة حول كل تعريف منهم. لذلك، نصيحتي هي عدم التركيز في تعريف كل نوع ومتى يستخدم، واهتم بمعرفة الفكرة بشكل عام لتفهم ماذا يقال عندما تذكر هذه المصطلحات.

أما عن ماذا تستخدم عند كتابة اختبار المكونات، فالحقيقة أنك ستنظر لإطار العمل المستخدم في مشروعك، وترى ماذا يعرض عليك من الأدوات، وتستخدمها لتحقق الغرض الأساسي من هذا كله، وهو **عزل الاختبارات عن أي عامل خارجي، بحيث تكون تحت بيئة محكمة، والسبب الوحيد لفشل الاختبار هو منطق الكود أو تنفيذه، وليس شيء آخر**.

مثال: إطار عمل [فيتست](https://vitest.dev/) هو أشهر إطار لاختبار المكونات حاليًا في عالم الجافا سكريبت، ويستخدم فقط دوبلير المحاكي (Mock) و الجاسوس و حتى هذين الأثنين متداخلين بشكل كبير، ولكنه يوفر كل ما تحتاجه، فلا توجد أي مشاكل.

## 🔎 زدنى

مقالة [مهمة و دسمة من الكاتب مارتن فاولر](https://martinfowler.com/articles/mocksArentStubs.html) حاول أن تقرأ منها على قدر ما تستطيع
